{
  "version": "0.2.0",
  "configurations": [
    // temporary debug configuration, we'll update this to use the usual plugin-sdk method soon
    // ! RUN `mage -v build:debug` AND RESTART GRAFANA BEFORE STARTING THE DEBUGGER !
    {
      "name": "Connect to docker",
      "type": "go",
      "request": "attach",
      "mode": "local",
      "debugAdapter": "dlv-dap",
      "substitutePath": []
    }
  ]
}
